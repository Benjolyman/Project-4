<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= build.name %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        .build-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 20px;
        }

        .build-header h1 {
            margin: 0;
        }

        .btn-group {
            display: flex;
            align-items: center;
        }

        .btn-group .btn {
            margin-left: 10px;
        }

        .build-container {
            max-width: 600px;
            margin: 0 auto;
        }

        .build-card {
            margin-bottom: 20px;
        }

        .card-body {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .card-title {
            margin: 0;
        }

        .container {
            margin-top: 90px;
        }
    </style>
</head>

<body>
    <%- include('../../partials/_navbar.ejs') %>

        <div class="container">
            <div class="build-container">
                <div class="build-header">
                    <h1>
                        <%= build.name %>
                    </h1>
                    <div class="btn-group">
                        <a href="/champions/<%= champions._id %>" class="btn btn-secondary me-2">Back</a>
                        <% if (build.owner._id.equals(user._id)) { %>
                            <a href="/champions/<%= champions._id %>/<%= build._id %>/edit" class="btn btn-primary">Edit
                                Build</a>
                            <% } %>
                    </div>
                </div>
                <div class="card build-card">
                    <div class="card-body">
                        <h3 class="card-title">Role: <%= build.role.name %>
                        </h3>
                    </div>
                </div>
                <div class="card build-card">
                    <div class="card-body">
                        <h3 class="card-title">Starter: <%= build.starterItem.name %>
                        </h3>
                    </div>
                </div>

                <div class="build-cards">
                    <% for (let i=0; i < 6; i++) { %>
                        <div class="card build-card">
                            <div class="card-body">
                                <h3 class="card-title">
                                    Item <%= i+1 %>: <%= build.items[i].name %>
                                </h3>
                            </div>
                        </div>
                        <% } %>

                            <div class="card build-card">
                                <div class="card-body">
                                    <h3 class="card-title">Rune 1: <%= build.primaryRune.name %>
                                    </h3>
                                </div>
                            </div>

                            <div class="card build-card">
                                <div class="card-body">
                                    <h3 class="card-title">Rune 2: <%= build.secondaryRune.name %>
                                    </h3>
                                </div>
                            </div>

                            <% for (let i=0; i < 2; i++) { %>
                                <div class="card build-card">
                                    <div class="card-body">
                                        <h3 class="card-title">
                                            Spell <%= i+1 %>: <%= build.summonerSpells[i].name %>
                                        </h3>
                                    </div>
                                </div>
                                <% } %>
                </div>
            </div>
        </div>

</body>

</html>